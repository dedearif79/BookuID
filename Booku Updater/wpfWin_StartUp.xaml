<Window x:Class="wpfWin_StartUp"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Booku_Updater"
        mc:Ignorable="d"
        Title="Booku Updater"
        Height="Auto"
        Width="Auto"
        WindowStartupLocation="CenterScreen"
        WindowStyle="SingleBorderWindow"
        ResizeMode="NoResize"
        >

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/BookuID.Styles;component/WPF/Styles/StyleAplikasi.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <StackPanel Style="{StaticResource stylePanelFormDialog}"   >

        <StackPanel Margin="15"  >


            <Grid   >

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="444"/>
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock      Grid.Row="0"  Grid.Column="0"   Style="{StaticResource styleTextBlockFormDialog}"   Name="lbl_01"       HorizontalAlignment="Center"    Text="Baris 01" />
                <TextBlock      Grid.Row="1"  Grid.Column="0"   Style="{StaticResource styleTextBlockFormDialog}"   Name="lbl_02"       HorizontalAlignment="Center"    Text="Baris 02" />
                <ProgressBar    Grid.Row="2"  Grid.Column="0"   Style="{StaticResource styleProgressBarFormDialog}" Name="pgb_Progress" Height="33"     Margin="0 15 0 0"   />
                <TextBlock      Grid.Row="3"  Grid.Column="0"   Style="{StaticResource styleTextBlockFormDialog}"   Name="lbl_Progress" Text="Progress"         />
            </Grid>

        </StackPanel>

        <!--Tombol CRUD :-->
        <StackPanel Margin="15"   Name="pnl_TombolForm">
            <Button Style="{StaticResource styleButtonFormDialogLanjutkan}" Name="btn_Lanjutkan" />
        </StackPanel>

    </StackPanel>

</Window>
