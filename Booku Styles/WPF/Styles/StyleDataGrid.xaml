<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- =================================================================== -->
    <!-- STYLE DATAGRID                                                      -->
    <!-- =================================================================== -->
    <!-- Semua warna menggunakan referensi dari StyleColor.xaml              -->
    <!-- =================================================================== -->


    <!-- ===== DATAGRID COLUMN HEADER ===== -->
    <Style x:Key="styleDataGridColumnHeader" TargetType="DataGridColumnHeader">
        <Setter Property="Background" Value="{StaticResource clrDataGridHeader}"/>
        <Setter Property="Foreground" Value="{StaticResource clrDataGridHeaderFg}"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="FontWeight" Value="Normal"/>
        <Setter Property="MinHeight" Value="27"/>
        <Setter Property="Padding" Value="3"/>
        <Setter Property="BorderBrush" Value="{StaticResource clrDataGridLine}"/>
        <Setter Property="BorderThickness" Value="0, 0, 1, 1"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <!--<Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DataGridColumnHeader">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource clrDataGridRowHover}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>-->
    </Style>


    <!-- ===== DATAGRID ROW ===== -->
    <Style x:Key="styleDataGridRow" TargetType="DataGridRow">
        <Setter Property="Background" Value="{StaticResource clrDataGridBg}"/>
        <Setter Property="Foreground" Value="{StaticResource clrTeksPrimer}"/>
        <Setter Property="BorderBrush" Value="{StaticResource clrDataGridLine}"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource clrDataGridRowHover}"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource clrDataGridRowSelect}"/>
                <Setter Property="Foreground" Value="{StaticResource clrDataGridRowSelectFg}"/>
            </Trigger>
            <Trigger Property="AlternationIndex" Value="1">
                <Setter Property="Background" Value="{StaticResource clrDataGridBgAlt}"/>
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="AlternationIndex" Value="1"/>
                    <Condition Property="IsSelected" Value="True"/>
                </MultiTrigger.Conditions>
                <Setter Property="Background" Value="{StaticResource clrDataGridRowSelect}"/>
                <Setter Property="Foreground" Value="{StaticResource clrDataGridRowSelectFg}"/>
            </MultiTrigger>
        </Style.Triggers>
    </Style>


    <!-- ===== DATAGRID CELL ===== -->
    <Style x:Key="styleDataGridCell" TargetType="DataGridCell">
        <Setter Property="Padding" Value="3"/>
        <Setter Property="BorderBrush" Value="{StaticResource clrDataGridLine}"/>
        <Setter Property="BorderThickness" Value="0,0,1,0"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, Path=Foreground}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DataGridCell">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource clrDataGridRowSelect}"/>
                <Setter Property="Foreground" Value="{StaticResource clrDataGridRowSelectFg}"/>
            </Trigger>
        </Style.Triggers>
    </Style>


    <!-- ===== DATAGRID DASAR ===== -->
    <Style x:Key="StyleDataGridDasar" TargetType="DataGrid">
        <Setter Property="ColumnHeaderStyle" Value="{StaticResource styleDataGridColumnHeader}"/>
        <Setter Property="RowStyle" Value="{StaticResource styleDataGridRow}"/>
        <Setter Property="CellStyle" Value="{StaticResource styleDataGridCell}"/>
        <Setter Property="RowBackground" Value="{StaticResource clrDataGridBg}"/>
        <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource clrDataGridLine}"/>
        <Setter Property="VerticalGridLinesBrush" Value="{StaticResource clrDataGridLine}"/>
        <Setter Property="Background" Value="{StaticResource clrDataGridBg}"/>
        <Setter Property="Foreground" Value="{StaticResource clrTeksPrimer}"/>
        <Setter Property="BorderBrush" Value="{StaticResource clrDataGridBorder}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="GridLinesVisibility" Value="All"/>
        <Setter Property="HeadersVisibility" Value="Column"/>
        <Setter Property="RowHeaderWidth" Value="0"/>
        <Setter Property="FontFamily" Value="Calibri Light"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="CanUserAddRows" Value="False"/>
        <Setter Property="CanUserDeleteRows" Value="False"/>
        <Setter Property="CanUserReorderColumns" Value="False"/>
        <Setter Property="CanUserResizeColumns" Value="True"/>
        <Setter Property="CanUserResizeRows" Value="False"/>
        <Setter Property="CanUserSortColumns" Value="False"/>
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="VerticalAlignment" Value="Top"/>
        <Setter Property="AutoGenerateColumns" Value="False"/>
        <Setter Property="IsReadOnly" Value="True"/>
        <Setter Property="SelectionMode" Value="Single"/>
        <Setter Property="SelectionUnit" Value="FullRow"/>
        <Setter Property="AlternationCount" Value="2"/>
    </Style>


    <!-- ===== DATAGRID MODUL (untuk UserControl) ===== -->
    <Style x:Key="StyleDataGridModul" TargetType="DataGrid" BasedOn="{StaticResource StyleDataGridDasar}">
        <Setter Property="Margin" Value="0"/>
    </Style>


    <!-- ===== DATAGRID FORM DIALOG (untuk Window) ===== -->
    <Style x:Key="StyleDataGridFormDialog" TargetType="DataGrid" BasedOn="{StaticResource StyleDataGridDasar}">
        <Setter Property="Margin" Value="0 5"/>
        <Setter Property="MinHeight" Value="100"/>
    </Style>


    <!-- ===== DATAGRID NOTA (untuk Invoice/Nota) ===== -->
    <Style x:Key="StyleDataGridNOTA" TargetType="DataGrid" BasedOn="{StaticResource StyleDataGridDasar}">
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Disabled"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="GridLinesVisibility" Value="Horizontal"/>
        <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource clrDataGridLine}"/>
    </Style>


    <!-- ===== DATAGRID COMPACT (untuk detail/child grid) ===== -->
    <Style x:Key="StyleDataGridCompact" TargetType="DataGrid" BasedOn="{StaticResource StyleDataGridDasar}">
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="RowHeight" Value="24"/>
    </Style>


</ResourceDictionary>
