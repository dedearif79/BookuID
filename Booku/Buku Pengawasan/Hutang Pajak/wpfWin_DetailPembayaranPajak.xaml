<Window x:Class="wpfWin_DetailPembayaranPajak"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Booku"
        mc:Ignorable="d"
        Title="Detail Pembayaran Pajak"
        Width="600"
        Height="500"
        MinWidth="500"
        MinHeight="400"
        WindowStartupLocation="CenterScreen"
        WindowStyle="SingleBorderWindow"
        ResizeMode="CanResize"
        >

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/BookuID.Styles;component/WPF/Styles/StyleAplikasi.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <DockPanel Style="{StaticResource stylePanelFormHalaman}">

        <!-- HEADER: Tombol -->
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 0 0 9">
            <Button Style="{StaticResource styleButtonFormHalaman}" x:Name="btn_LihatJurnal" Content="Lihat Jurnal" IsEnabled="False"/>
        </StackPanel>

        <!-- KONTEN: DataGrid -->
        <StackPanel Style="{StaticResource stylePanelDataGridView}">
            <DataGrid x:Name="datagridUtama"
                      AutoGenerateColumns="False"
                      MinWidth="500"
                      MinHeight="300"/>
        </StackPanel>

    </DockPanel>

</Window>
