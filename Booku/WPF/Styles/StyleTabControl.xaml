<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ===== WARNA TEMA TAB CONTROL (BOOKU Green Branding) ===== -->
    <SolidColorBrush x:Key="TabControlBackground" Color="#F5F5F5"/>
    <SolidColorBrush x:Key="TabControlBorderBrush" Color="#C8E6C9"/>
    <SolidColorBrush x:Key="TabItemBackground" Color="#EEEEEE"/>
    <SolidColorBrush x:Key="TabItemSelectedBackground" Color="White"/>
    <SolidColorBrush x:Key="TabItemHoverBackground" Color="#E8F5E9"/>
    <SolidColorBrush x:Key="TabItemForeground" Color="#333333"/>
    <SolidColorBrush x:Key="TabItemSelectedForeground" Color="#388E3C"/>
    <SolidColorBrush x:Key="TabItemBorderBrush" Color="#A5D6A7"/>
    <SolidColorBrush x:Key="TabContentBackground" Color="White"/>

    <!-- ===== TAB CONTROL DASAR ===== -->
    <Style x:Key="styleTabControlDasar" TargetType="TabControl">
        <Setter Property="Background" Value="{StaticResource TabControlBackground}"/>
        <Setter Property="BorderBrush" Value="{StaticResource TabControlBorderBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
    </Style>

    <!-- Tab Control untuk Main Content (Application Shell) -->
    <Style x:Key="styleTabControlMainContent" BasedOn="{StaticResource styleTabControlDasar}" TargetType="TabControl">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabControl">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Tab Headers dengan ScrollViewer untuk banyak tab -->
                        <Border Grid.Row="0"
                                Background="{StaticResource TabControlBackground}"
                                BorderBrush="{StaticResource TabControlBorderBrush}"
                                BorderThickness="0,0,0,1">
                            <ScrollViewer HorizontalScrollBarVisibility="Auto"
                                          VerticalScrollBarVisibility="Disabled"
                                          CanContentScroll="True">
                                <TabPanel IsItemsHost="True"
                                          Margin="5,5,5,0"/>
                            </ScrollViewer>
                        </Border>

                        <!-- Tab Content -->
                        <Border Grid.Row="1"
                                Background="{StaticResource TabContentBackground}"
                                BorderBrush="{StaticResource TabControlBorderBrush}"
                                BorderThickness="0">
                            <ContentPresenter ContentSource="SelectedContent"
                                              Margin="0"/>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===== TAB ITEM DASAR ===== -->
    <Style x:Key="styleTabItemDasar" TargetType="TabItem">
        <Setter Property="Foreground" Value="{StaticResource TabItemForeground}"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="Margin" Value="0,0,2,0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Border x:Name="Border"
                            Background="{StaticResource TabItemBackground}"
                            BorderBrush="{StaticResource TabItemBorderBrush}"
                            BorderThickness="1,1,1,0"
                            CornerRadius="4,4,0,0"
                            Padding="{TemplateBinding Padding}"
                            Margin="{TemplateBinding Margin}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter ContentSource="Header"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource TabItemSelectedBackground}"/>
                            <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource TabControlBorderBrush}"/>
                            <Setter TargetName="Border" Property="Margin" Value="0,0,2,-1"/>
                            <Setter Property="Foreground" Value="{StaticResource TabItemSelectedForeground}"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource TabItemHoverBackground}"/>
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected" Value="True"/>
                                <Condition Property="IsMouseOver" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource TabItemSelectedBackground}"/>
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Tab Item untuk Main Content -->
    <Style x:Key="styleTabItemMainContent" BasedOn="{StaticResource styleTabItemDasar}" TargetType="TabItem">
        <Setter Property="MinWidth" Value="100"/>
        <Setter Property="MaxWidth" Value="200"/>
    </Style>

    <!-- ===== BUTTON CLOSE TAB ===== -->
    <Style x:Key="styleButtonCloseTab" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Foreground" Value="#757575"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Padding" Value="4,0"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Border"
                            Background="{TemplateBinding Background}"
                            CornerRadius="2"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="#FFCDD2"/>
                            <Setter Property="Foreground" Value="#D32F2F"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="#EF9A9A"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
