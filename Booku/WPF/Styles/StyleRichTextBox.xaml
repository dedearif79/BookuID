<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Booku">

    <!-- =================================================================== -->
    <!-- STYLE RICHTEXTBOX                                                   -->
    <!-- =================================================================== -->
    <!-- Semua warna menggunakan referensi dari StyleColor.xaml              -->
    <!-- =================================================================== -->


    <!-- ===== RICHTEXTBOX DASAR ===== -->
    <Style x:Key="styleRichTextBoxDasar" TargetType="RichTextBox">
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Foreground" Value="{StaticResource clrTeksPrimer}"/>
        <Setter Property="Background" Value="{StaticResource clrBgInput}"/>
        <Setter Property="BorderBrush" Value="{StaticResource clrBorderDefault}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="VerticalContentAlignment" Value="Top"/>
        <Setter Property="Opacity" Value="1"/>
        <Setter Property="Padding" Value="10,8"/>
        <Setter Property="AcceptsReturn" Value="True"/>
        <Setter Property="local:RichTextBoxBehavior.KarakterDilarang" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RichTextBox">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="6"
                            SnapsToDevicePixels="True">
                        <ScrollViewer x:Name="PART_ContentHost"
                                      Focusable="False"
                                      HorizontalScrollBarVisibility="Hidden"
                                      VerticalScrollBarVisibility="Auto"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource clrBorderFocus}"/>
                        </Trigger>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource clrBorderFocus}"/>
                        </Trigger>
                        <Trigger Property="IsReadOnly" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource clrBgInputReadOnly}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource clrBgInputDisabled}"/>
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource clrBorderDisabled}"/>
                            <Setter Property="Foreground" Value="{StaticResource clrTeksDisabled}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <!-- =================================================================== -->
    <!-- RICHTEXTBOX FORM HALAMAN (UserControl)                              -->
    <!-- =================================================================== -->

    <!-- RichTextBox Form Halaman - Base -->
    <Style x:Key="styleRichTextBoxFormHalaman" BasedOn="{StaticResource styleRichTextBoxDasar}" TargetType="RichTextBox">
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="DockPanel.HorizontalAlignment" Value="Stretch"/>
        <Setter Property="Height" Value="Auto"/>
        <Setter Property="MinHeight" Value="60"/>
        <Setter Property="Margin" Value="0 5"/>
    </Style>


    <!-- =================================================================== -->
    <!-- RICHTEXTBOX FORM DIALOG (Window)                                    -->
    <!-- =================================================================== -->

    <!-- RichTextBox Form Dialog - Base -->
    <Style x:Key="styleRichTextBoxFormDialog" BasedOn="{StaticResource styleRichTextBoxDasar}" TargetType="RichTextBox">
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="DockPanel.HorizontalAlignment" Value="Stretch"/>
        <Setter Property="Height" Value="Auto"/>
        <Setter Property="MinHeight" Value="60"/>
        <Setter Property="MaxHeight" Value="120"/>
        <Setter Property="Margin" Value="0 5"/>
        <Setter Property="Padding" Value="10,8"/>
    </Style>

    <!-- RichTextBox Form Dialog - Kecil -->
    <Style x:Key="styleRichTextBoxFormDialogKecil" BasedOn="{StaticResource styleRichTextBoxFormDialog}" TargetType="RichTextBox">
        <Setter Property="MinHeight" Value="40"/>
        <Setter Property="MaxHeight" Value="80"/>
    </Style>

    <!-- RichTextBox Form Dialog - Besar -->
    <Style x:Key="styleRichTextBoxFormDialogBesar" BasedOn="{StaticResource styleRichTextBoxFormDialog}" TargetType="RichTextBox">
        <Setter Property="MinHeight" Value="100"/>
        <Setter Property="MaxHeight" Value="200"/>
    </Style>


</ResourceDictionary>
